# ===========================================
# DATABASE BACKEND
# ===========================================
# "rest" = Supabase REST API (works over HTTPS/443, recommended)
# "postgres" = Direct PostgreSQL (requires port 5432 open)
DB_BACKEND=rest

# ===========================================
# REST API CONFIG (for DB_BACKEND=rest)
# ===========================================
SUPABASE_URL=...
SUPABASE_SERVICE_KEY=your-service-role-key

# ===========================================
# POSTGRES CONFIG (for DB_BACKEND=postgres)
# ===========================================
# PG_DSN=postgresql://postgres:PASSWORD@.../postgres?sslmode=require

# ===========================================
# S3/SUPABASE STORAGE
# ===========================================
S3_ENDPOINT=...
S3_ACCESS_KEY=your-access-key
S3_SECRET_KEY=your-secret-key
S3_BUCKET=files
S3_REGION=eu-central-1

# ===========================================
# SYNC SETTINGS
# ===========================================
# These are CONTAINER-INTERNAL paths (after volume mount)
# Example: if you mount /volume1/projects:/data/projects, use /data/projects here
SYNC_SOURCE_PATHS=/data/projects,/data/documents

# Sync interval in seconds (default: 900 = 15 minutes)
SYNC_INTERVAL=900

# ===========================================
# LOGGING
# ===========================================
LOG_LEVEL=INFO
BETTERSTACK_SOURCE_TOKEN=
BETTERSTACK_INGEST_HOST=

# ===========================================
# TIMEZONE
# ===========================================
TIMEZONE=Europe/Berlin
