# ===========================================
# FileMetadataSync Configuration
# ===========================================

# Supabase (required)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key

# Storage bucket name (default: files)
S3_BUCKET=files

# Number of worker threads (default: 6)
# DS923+: 4-6 recommended
SYNC_WORKERS=6

# ===========================================
# Watcher Settings
# ===========================================

# Debounce time in seconds (default: 3.0)
# Events within this window are batched together
DEBOUNCE_SECONDS=3.0

# Patterns to ignore (comma-separated, default: common temp files)
# Leave empty to use defaults: *.tmp,*.temp,.DS_Store,Thumbs.db,*.partial,.syncing,@eaDir/*,#recycle/*,.SynologyWorkingDirectory/*
IGNORE_PATTERNS=

# ===========================================
# Full Scan Schedule
# ===========================================

# Hour of day for daily full scan (0-23, default: 3 = 3 AM)
FULL_SCAN_HOUR=3

# Run full scan on container startup (default: true)
# Set to false if you want watcher-only mode on restart
FULL_SCAN_ON_STARTUP=true

# ===========================================
# Logging
# ===========================================

LOG_LEVEL=INFO
BETTERSTACK_SOURCE_TOKEN=
BETTERSTACK_INGEST_HOST=

# Timezone (default: Europe/Berlin)
TIMEZONE=Europe/Berlin

# ===========================================
# Advanced (usually not needed)
# ===========================================

# Override scan paths (default: /data)
# Only set this if you need custom paths inside the container
# SYNC_SOURCE_PATHS=/data
